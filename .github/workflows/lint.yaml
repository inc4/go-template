name: Linter

on: push

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: golangci/golangci-lint-action@v3

      - run: go version

      - name: Run linters
        run: make lint
